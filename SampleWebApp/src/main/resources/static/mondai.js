function start() {
	console.log("Hello");
	// 名前で複数のオブジェクトを取得する
	let fruits = document.getElementsByName("fruit");

   for ( var i = 0; i < fruits.length; i++) {
	   if ((fruits[i].checked) {
	       // ここで、チェックされたものの処理
	   }
   }
}
